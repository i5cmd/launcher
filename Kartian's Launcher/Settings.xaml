<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Kartian_s_Launcher.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kartian_s_Launcher"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Settings">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <ListView Grid.Row="1" x:Name="Shortcuts" ItemsSource="{x:Bind shortcuters}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ShortcutDetails">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontWeight="Bold" Text="{x:Bind app.Name}"></TextBlock>
                        <TextBlock FontWeight="Bold" Text="{x:Bind inputsOne}"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
