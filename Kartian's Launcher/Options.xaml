<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Kartian_s_Launcher.Options"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kartian_s_Launcher"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Options">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ToggleSwitch Header="The X button" OffContent="Closes the program" OnContent="Hides the window" x:Name="CloseWin"></ToggleSwitch>
                <TextBlock FontSize="10" Text="Hotkeys will work in the background only when the window is hidden."></TextBlock>
                <Rectangle Height="1" Fill="#333333" HorizontalAlignment="Stretch" Margin="0 10" />
                <ToggleSwitch Header="Ctrl+Alt+Home hotkey" OffContent="Does nothing" OnContent="Hides or shows the window" x:Name="HotkeyWin"></ToggleSwitch>
            </StackPanel>
        </ScrollViewer>
        <StackPanel Grid.Row="1">
            <Button Content="Save" Click="SaveEditedContent_Click" Style="{StaticResource AccentButtonStyle}" x:Name="SaveEditedContent" HorizontalAlignment="Center" Margin="5"></Button>
        </StackPanel>
    </Grid>
</Window>
